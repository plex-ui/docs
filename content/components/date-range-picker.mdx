---
title: Date Range Picker
description: Select a date range with an interactive calendar
---

import {
  DateRangePickerBaseDemo,
  DateRangePickerLimitsDemo,
  DateRangePickerShortcutsDemo,
  DateRangePickerMonthStepperDemoWithControls,
} from "@/components/docs/DateRangePickerDemos"

<UsageBlock>{`import { DateRangePicker } from "@plexui/ui/components/DateRangePicker";`}</UsageBlock>

## Examples

### Overview

<ComponentPreview code={`const [value, setValue] = useState<DateRange | null>(null)
<DateRangePicker value={value} onChange={setValue} shortcuts={shortcuts} clearable pill />`} previewMinHeight={168}>
  <DateRangePickerBaseDemo />
</ComponentPreview>

### Limits

<ComponentPreview details={<>Use <code>min</code> and <code>max</code> to limit the selectable range. Use <code>maxRangeDays</code> to limit the range length.</>} code={`<DateRangePicker value={...} onChange={...} min={minDate} max={maxDate} />`} previewMinHeight={168}>
  <DateRangePickerLimitsDemo />
</ComponentPreview>


### Shortcuts

<ComponentPreview details={<>Use the <code>shortcuts</code> array to provide quick date range options. When a shortcut is selected, its label is shown in the trigger.</>} code={`<DateRangePicker shortcuts={dayShortcuts} ... />`} previewMinHeight={168}>
  <DateRangePickerShortcutsDemo />
</ComponentPreview>


### Month stepper

<ComponentPreview details={<>Enable a stepper in the trigger with <code>triggerStepperUnit="month"</code>. When the selected range matches the unit, the trigger shows the stepper.</>} code={`<DateRangePicker
  triggerStepperUnit="month"
  value={value}
  onChange={setValue}
  clearable
  pill
  max={maxDate}
/>`} previewMinHeight={168}>
  <DateRangePickerMonthStepperDemoWithControls />
</ComponentPreview>

## Reference

<PropsTable data={[
  { prop: "value", type: "DateRange | null", description: "Selected range ([start, end], inclusive)" },
  { prop: "onChange", type: "(next: DateRange | null, shortcut?: DateRangeShortcut) => void", description: "Called when range changes" },
  { prop: "min", type: "DateTime", description: "Earliest selectable date" },
  { prop: "max", type: "DateTime", description: "Latest selectable date" },
  { prop: "maxRangeDays", type: "number", description: "Max length of range in days (inclusive)" },
  { prop: "shortcuts", type: "DateRangeShortcut[]", description: "Quick selection shortcuts" },
  { prop: "side", type: '"top" | "right" | "bottom" | "left"', default: '"bottom"', description: "Preferred side for popover" },
  { prop: "sideOffset", type: "number", default: "8", description: "Distance from trigger in pixels" },
  { prop: "align", type: '"start" | "center" | "end"', default: '"center"', description: "Preferred popover alignment" },
  { prop: "alignOffset", type: "number", default: "0", description: "Offset from start/end alignment in pixels" },
  { prop: "disabled", type: "boolean", default: "false", description: "Disable picker interactions" },
  { prop: "placeholder", type: "string", default: '"Select date range..."', description: "Placeholder in trigger when value is empty" },
  { prop: "variant", type: '"soft" | "outline" | "ghost"', default: '"outline"', description: "Trigger style variant" },
  { prop: "pill", type: "boolean", default: "true", description: "Rounded trigger shape" },
  { prop: "size", type: '"3xs" | "2xs" | "xs" | "sm" | "md" | "lg" | "xl" | "2xl" | "3xl"', default: '"md"', description: "Trigger size scale" },
  { prop: "dropdownIconType", type: '"chevronDown" | "dropdown" | "none"', default: '"dropdown"', description: "Icon shown at trigger end" },
  { prop: "triggerClassName", type: "string", description: "Custom class for trigger" },
  { prop: "triggerShowIcon", type: "boolean", default: "true", description: "Show calendar icon at trigger start" },
  { prop: "triggerStepperUnit", type: '"month"', description: "Enable month stepper mode in trigger" },
  { prop: "triggerDateFormat", type: "string", default: '"MM/dd/yy"', description: "Date formatting in trigger text" },
  { prop: "clearable", type: "boolean", default: "false", description: "Show clear button in trigger" },
  { prop: "block", type: "boolean", default: "false", description: "Stretch trigger to full available width" },
]} />
