---
title: Form Examples
description: Production-ready React form examples — sign-up, log-in, password reset, email and phone verification, OAuth, and file upload built with Plex UI
---

import {
  FloatingLabelInputPhoneSignupFormDemo,
  FloatingLabelInputPhoneLoginFormDemo,
  FloatingLabelInputPasswordFormDemo,
  FloatingLabelSelectAboutYouFormDemo,
} from "@/components/docs/FloatingLabelInputDemos"
import {
  OTPInputCheckInboxErrorDemo,
  OTPInputVerifyEmailDemo,
  OTPInputConfirmPhoneDemo,
} from "@/components/docs/OTPInputDemos"
import {
  OAuthLoginFormDemo,
  OAuthSignupFormDemo,
} from "@/components/docs/OAuthFormDemos"
import { FileUploadFormDemo } from "@/components/docs/FileUploadDemos"


Real-world forms composed from [Float Input](/components/floating-label-input), [File Upload](/components/file-upload), [OTP Input](/components/otp-input), [Select](/components/select), and [Button](/components/button).

## Log in (OAuth)

<ComponentPreview details={<>OpenAI-style log-in: <code>Welcome back</code> heading, email + <code>Continue</code>, a <code>Don&apos;t have an account? Sign up</code> cross-link, an <code>OR</code> divider, then Google, Apple, Microsoft, and phone — all with 20×20 brand glyphs. <code>Terms of Use</code> / <code>Privacy Policy</code> footer.</>} code={`<h3>Welcome back</h3>

<form onSubmit={handleSubmit}>
  <FloatingLabelInput label="Email address" type="email" />
  <Button color="primary" size="3xl" pill block>Continue</Button>
</form>

<p>Don't have an account? <a>Sign up</a></p>

<OrDivider />

<Button color="secondary" variant="outline" size="3xl" pill block>
  <GoogleLogo /> Continue with Google
</Button>
<Button color="secondary" variant="outline" size="3xl" pill block>
  <AppleLogo /> Continue with Apple
</Button>
<Button color="secondary" variant="outline" size="3xl" pill block>
  <MicrosoftLogo /> Continue with Microsoft
</Button>
<Button color="secondary" variant="outline" size="3xl" pill block>
  <PhoneLogo /> Continue with phone
</Button>

<p><a>Terms of Use</a> | <a>Privacy Policy</a></p>`} previewMinHeight={760}>
  <OAuthLoginFormDemo />
</ComponentPreview>

## Sign up (OAuth)

<ComponentPreview details={<>OpenAI-style sign-up: <code>Create an account</code> heading, email + <code>Continue</code>, an <code>Already have an account? Log in</code> cross-link, an <code>OR</code> divider, then Google, Apple, and Microsoft (no phone). Same 20×20 brand-glyph alignment as the log-in variant.</>} code={`<h3>Create an account</h3>

<form onSubmit={handleSubmit}>
  <FloatingLabelInput label="Email address" type="email" />
  <Button color="primary" size="3xl" pill block>Continue</Button>
</form>

<p>Already have an account? <a>Log in</a></p>

<OrDivider />

<Button color="secondary" variant="outline" size="3xl" pill block>
  <GoogleLogo /> Continue with Google
</Button>
<Button color="secondary" variant="outline" size="3xl" pill block>
  <AppleLogo /> Continue with Apple
</Button>
<Button color="secondary" variant="outline" size="3xl" pill block>
  <MicrosoftLogo /> Continue with Microsoft
</Button>

<p><a>Terms of Use</a> | <a>Privacy Policy</a></p>`} previewMinHeight={680}>
  <OAuthSignupFormDemo />
</ComponentPreview>

## Sign up (phone)

<ComponentPreview details={<>A complete signup form with floating-label text inputs, a country code selector, and a phone input — all at 52px height. Includes form validation and terms text.</>} code={`<form className="flex flex-col gap-3">
  <FloatingLabelInput label="Full name" />
  <FloatingLabelInput label="Email address" type="email" />
  <Select trigger={countryTrigger} ... />
  <FloatingLabelInput label="Phone number" startAdornment={<span>+34</span>} type="tel" />
  <p>Terms and Privacy Policy</p>
  <Button color="primary" size="3xl" pill className="h-[3.25rem]">Continue</Button>
</form>`} previewMinHeight={580}>
  <FloatingLabelInputPhoneSignupFormDemo />
</ComponentPreview>

## Log in (phone)

<ComponentPreview details={<>A phone login form with a country code selector and phone input. Minimal layout matching the OpenAI login pattern.</>} code={`<form className="flex flex-col gap-3">
  <Select trigger={countryTrigger} ... />
  <FloatingLabelInput label="Phone number" startAdornment={<span>+34</span>} type="tel" />
  <Button color="primary" size="3xl" pill className="h-[3.25rem]">Continue</Button>
</form>`} previewMinHeight={420}>
  <FloatingLabelInputPhoneLoginFormDemo />
</ComponentPreview>

## Enter password

<ComponentPreview details={<>A password entry step with a read-only phone field (with "Edit" button), a password input with visibility toggle and tooltip, and a "Forgot password?" link.</>} code={`<form className="flex flex-col gap-3">
  <FloatingLabelInput label="Phone number" readOnly defaultValue="+1 555 123 4567" endAdornment={<button>Edit</button>} />
  <FloatingLabelInput label="Password" type="password" endAdornment={<VisibilityToggle />} />
  <Button color="primary" size="3xl" pill className="h-[3.25rem]">Continue</Button>
</form>`} previewMinHeight={420}>
  <FloatingLabelInputPasswordFormDemo />
</ComponentPreview>

## About You

<ComponentPreview details={<>A signup-style composition with <code>FloatingLabelInput</code> and <code>FloatingLabelSelect</code> — name, birthday with inline validation, and country selector.</>} code={`<form className="flex flex-col gap-3">
  <FloatingLabelInput label="Full name" />
  <FloatingLabelInput label="Birthday" placeholder="MM / DD / YYYY" />
  <FloatingLabelSelect label="Country" selected={!!country} ...>
    {selectedCountry?.flag} {selectedCountry?.label}
  </FloatingLabelSelect>
  <Button color="primary" size="3xl" type="submit">Continue</Button>
</form>`} previewMinHeight={280}>
  <FloatingLabelSelectAboutYouFormDemo />
</ComponentPreview>

## Verify email

<ComponentPreview details={<>Toggle between a per-digit <code>OTPInput</code> (auto-submits via <code>onComplete</code>) and a standard <code>FloatingLabelInput</code> (for alphanumeric or variable-length codes).</>} code={`// Per-digit
<OTPInput autoFocus />

// Text field
<FloatingLabelInput label="Code" inputMode="numeric" />
<Button color="primary" size="3xl" pill className="h-[3.25rem]">Continue</Button>`} previewMinHeight={380}>
  <OTPInputVerifyEmailDemo />
</ComponentPreview>

## Verify email (error)

<ComponentPreview details={<>Error state when an incorrect verification code is submitted.</>} code={`<FloatingLabelInput label="Code" value="111111" invalid errorMessage="Incorrect code" />`} previewMinHeight={380}>
  <OTPInputCheckInboxErrorDemo />
</ComponentPreview>

## Confirm phone

<ComponentPreview details={<>Toggle between a per-digit <code>OTPInput</code> and a standard <code>FloatingLabelInput</code> for phone verification.</>} code={`// Per-digit
<OTPInput autoFocus />

// Text field
<FloatingLabelInput label="Code" inputMode="numeric" />
<Button color="primary" size="3xl" pill className="h-[3.25rem]">Continue</Button>`} previewMinHeight={380}>
  <OTPInputConfirmPhoneDemo />
</ComponentPreview>

## Upload file

<ComponentPreview details={<>A document upload form with <code>FileUpload</code>. The file list grows dynamically as files are added. Button and file list share the same 52px pill shape.</>} code={`<FileUpload accept=".pdf,.png" multiple maxFiles={5} />
<Button color="primary" size="3xl" pill block>Continue</Button>`}>
  <FileUploadFormDemo />
</ComponentPreview>
