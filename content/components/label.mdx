---
title: Label
description: Accessible React label component that wires up to any form control via htmlFor, with consistent typography and Field-aware integration
---

import { Label } from "@plexui/ui/components/Label"
import {
  LabelOverviewDemo,
  LabelInFieldDemo,
} from "@/components/docs/LabelDemos"

<UsageBlock>{`import { Label } from "@plexui/ui/components/Label";`}</UsageBlock>

For stacked form fields, prefer [`Field`](/components/field) — it bundles label, description, and error handling. Reach for a standalone `Label` when you pair it directly with a checkbox, switch, or any control where Field's layout is overkill.

## Examples

### Overview

<ComponentPreview code={`<div style={{ display: 'flex', alignItems: 'center', gap: 8 }}>
  <Checkbox id="terms" />
  <Label htmlFor="terms">Accept terms and conditions</Label>
</div>`}>
  <LabelOverviewDemo />
</ComponentPreview>

### Label in Field

<ComponentPreview details={<>For stacked form rows with descriptions, errors, and horizontal layouts, use the <a href="/components/field"><code>Field</code></a> component — it renders the label internally, hooks up <code>htmlFor</code> automatically, and handles the surrounding spacing.</>} code={`<Field label="Name on Card">
  <Input placeholder="Aisha Cooper" />
</Field>

<Field label="Card Number" description="Enter your 16-digit card number">
  <Input placeholder="1234 5678 9012 3456" />
</Field>

{/* Month / Year / CVV in a 3-col grid */}
{/* Billing Address section + checkbox */}
{/* Comments textarea + Submit / Cancel */}`}>
  <LabelInFieldDemo />
</ComponentPreview>

## API

### Label

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `htmlFor` | `string` | — | `id` of the associated control. Clicks on the label focus or activate the control. |
| `className` | `string` | — | Applied to the root `<label>` element |

Built on [Radix Label](https://www.radix-ui.com/primitives/components/label) — prevents double-click text selection and forwards clicks to the associated control. All other `<label>` attributes are forwarded.

## Styling

Component tokens:

- `--label-color` — text color (defaults to `--color-text`)
- `--label-font-size` — text size (defaults to `--font-text-sm-size`)
- `--label-font-weight` — weight (defaults to `--font-weight-medium`)
- `--label-line-height` — line height (defaults to `1`)
