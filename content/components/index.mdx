---
title: Components
description: Browse the full Plex UI React component library — buttons, inputs, dialogs, data tables, navigation, and form primitives with live demos
---

import { ComponentsList } from '@/components/docs/ComponentsList';

Here you can find all the components available in Plex UI. We're working on adding more — if something's missing, [open an issue on GitHub](https://github.com/plex-ui/docs/issues/new).

<ComponentsList />
