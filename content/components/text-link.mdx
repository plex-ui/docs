---
title: Text Link
description: Semantic link for internal and external links
---

import {
  TextLinkBaseDemo,
  TextLinkColorsDemo,
  TextLinkPrimaryDemo,
  TextLinkUnderlineDemo,
  TextLinkExternalDemo,
  TextLinkWeightDemo,
} from '@/components/docs/TextLinkDemos'
import { TextLink } from '@plexui/ui/components/TextLink'
import { ArrowUpRight } from '@plexui/ui/components/Icon'

<UsageBlock>{`import { TextLink } from "@plexui/ui/components/TextLink";`}</UsageBlock>

## Examples

### Overview

<ComponentPreview code={`<div className="flex gap-4">
  <TextLink href="#">Read more</TextLink>
  <TextLink href="#" primary>External link <ArrowUpRight /></TextLink>
</div>`} previewMinHeight={168}>
  <div className="flex gap-4">
    <TextLink href="#">Read more</TextLink>
    <TextLink href="#" primary>External link <ArrowUpRight /></TextLink>
  </div>
</ComponentPreview>

### Inline usage

<ComponentPreview code={`<p>
  You can use the <TextLink href="#">responses endpoint</TextLink> to generate text.
  Or use one of the <TextLink href="#">official SDKs</TextLink> for your language.
</p>`} previewMinHeight={168}>
  <TextLinkBaseDemo />
</ComponentPreview>

### Default inheritance

<ComponentPreview details={<>By default, <code>TextLink</code> uses <code>currentcolor</code> inheritance and <code>text-decoration: underline</code> to create subtle visual differentiation from surrounding text. The hover effect is a dynamic deemphasis of the <code>currentcolor</code>, with some special cases for <code>text-secondary</code> and <code>text-tertiary</code> contexts.<br /><br />You can see this behavior by changing the text color context of the paragraph the links are in below:</>} code={`<p className="text-secondary">
  <TextLink href="#">responses endpoint</TextLink> ... <TextLink href="#">official SDKs</TextLink>
</p>`} previewMinHeight={168}>
  <TextLinkColorsDemo />
</ComponentPreview>

### Primary color

<ComponentPreview details={<>You can create a colorful link style with the <code>primary</code> prop, which uses color for visual differentiation.<br /><br />Primary links also remove the default <code>underline</code> treatment - pass <code>underline</code> re-apply that style.</>} code={`<TextLink href="#" primary>primary link</TextLink>`} previewMinHeight={168}>
  <TextLinkPrimaryDemo />
</ComponentPreview>

### Underline

<ComponentPreview details={<>Links have an underlined aesthetic by default.<br /><br />Pass <code>{'underline={false}'}</code> to remove the underline from the link.</>} code={`<TextLink href="#" underline={false}>subtle link</TextLink>`} previewMinHeight={168}>
  <TextLinkUnderlineDemo />
</ComponentPreview>

### External links

<ComponentPreview details={<>When external paths are provided, <code>{'<a>'}</code> will be used with <code>{'target="_blank"'}</code> and <code>{'rel="noopener noreferrer"'}</code> included by default. You can also compose icons along with the text to add visual indication.</>} code={`<TextLink href="#" primary underline>
  External link <ArrowUpRight />
</TextLink>`} previewMinHeight={168}>
  <TextLinkExternalDemo />
</ComponentPreview>

### Font weight

<ComponentPreview details={<>Use the <code>className</code> prop to customize font weight.</>} code={`<TextLink href="#" className="font-semibold" underline={false}>
  Contact support <ArrowRight />
</TextLink>`} previewMinHeight={168}>
  <TextLinkWeightDemo />
</ComponentPreview>

## Reference

<PropsTable data={[
  { prop: "children", type: "ReactNode", description: "Link content" },
  { prop: "href", type: "string", description: "URL" },
  { prop: "to", type: "object", description: "Router link destination (internal)" },
  { prop: "primary", type: "boolean", default: "false", description: "Primary color, no default underline" },
  { prop: "underline", type: "boolean", default: "true", description: "Underline (default true unless primary)" },
  { prop: "forceExternal", type: "boolean", description: "Force external link behavior" },
  { prop: "as", type: "Component", description: "Link component (e.g. Next Link)" },
]} />

### Configure your Router

Wrap your app in `PlexUIProvider` and pass your router's Link component. You can also pass the Link component to the `as` prop on `TextLink` for component-level override.

```tsx
import { PlexUIProvider } from "@plexui/ui/components/PlexUIProvider"
import { Link } from "react-router"

export function App() {
  return (
    <PlexUIProvider linkComponent={Link}>
      ...
    </PlexUIProvider>
  )
}

<TextLink to={{ pathname: "/help" }} prefetch="intent">Get Help</TextLink>
```

```tsx
import { Link } from "next/link"

<TextLink as={Link} href="/dashboard">View Dashboard</TextLink>
```
