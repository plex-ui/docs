---
title: Button Group
description: Join buttons into a single visual unit — toolbars, steppers, split buttons, search affordances
---

import { ButtonGroup } from "@plexui/ui/components/ButtonGroup"
import {
  ButtonGroupOverviewDemo,
  ButtonGroupOrientationDemo,
  ButtonGroupSizeDemo,
  ButtonGroupNestedDemo,
  ButtonGroupSeparatorDemo,
  ButtonGroupSplitDemo,
  ButtonGroupInputDemo,
  ButtonGroupInputGroupDemo,
  ButtonGroupDropdownMenuDemo,
  ButtonGroupSelectDemo,
  ButtonGroupPopoverDemo,
} from "@/components/docs/ButtonGroupDemos.client"

<UsageBlock>{`import { ButtonGroup } from "@plexui/ui/components/ButtonGroup";`}</UsageBlock>

Wraps any row of controls — `Button`, `Input`, `Popover.Trigger`, etc. — and removes the shared border so they read as one unit. Nest `ButtonGroup` inside itself to create visually separated sub-groups (the outer group automatically spaces them).

## Examples

### Overview

<ComponentPreview code={`<ButtonGroup>
  <ButtonGroup><Button uniform aria-label="Go back"><ArrowLeftSm /></Button></ButtonGroup>
  <ButtonGroup>
    <Button>Pin</Button>
    <Button>Tag</Button>
  </ButtonGroup>
  <ButtonGroup>
    <Button>Bookmark</Button>
    <Button uniform aria-label="More options"><DotsHorizontal /></Button>
  </ButtonGroup>
</ButtonGroup>`}>
  <ButtonGroupOverviewDemo />
</ComponentPreview>

### Orientation

<ComponentPreview details={<>Pass <code>orientation="vertical"</code> to stack the buttons — useful for <code>+</code> / <code>−</code> steppers, zoom controls, or any vertical toolbar.</>} code={`<ButtonGroup orientation="vertical" aria-label="Zoom controls">
  <Button uniform aria-label="Zoom in"><PlusSm /></Button>
  <Button uniform aria-label="Zoom out"><Minus /></Button>
</ButtonGroup>`}>
  <ButtonGroupOrientationDemo />
</ComponentPreview>

### Size

<ComponentPreview details={<>Set <code>size</code> on every <code>Button</code> inside the group so corner radii match. Mix icon-only (<code>uniform</code>) with text buttons — each child keeps its own padding.</>} code={`<ButtonGroup>
  <Button size="sm">Small</Button>
  <Button size="sm">Button</Button>
  <Button size="sm">Group</Button>
  <Button size="sm" uniform aria-label="Add"><PlusSm /></Button>
</ButtonGroup>`}>
  <ButtonGroupSizeDemo />
</ComponentPreview>

### Nested

<ComponentPreview details={<>Place <code>ButtonGroup</code> inside <code>ButtonGroup</code> to create visually separated sub-groups. The outer group automatically adds a small gap between children; pagination + arrow nav is the canonical pattern.</>} code={`<ButtonGroup aria-label="Pager">
  <ButtonGroup>
    <Button size="sm">1</Button>
    <Button size="sm">2</Button>
    <Button size="sm">3</Button>
    <Button size="sm">4</Button>
    <Button size="sm">5</Button>
  </ButtonGroup>
  <ButtonGroup>
    <Button size="sm" uniform aria-label="Previous page"><ArrowLeftSm /></Button>
    <Button size="sm" uniform aria-label="Next page"><ArrowRightSm /></Button>
  </ButtonGroup>
</ButtonGroup>`}>
  <ButtonGroupNestedDemo />
</ComponentPreview>

### Separator

<ComponentPreview details={<>Drop a <code>ButtonGroup.Separator</code> between items to add a visible divider — commonly used for Undo / Redo pairs or any "two related actions" toolbar.</>} code={`<ButtonGroup>
  <Button variant="soft" color="secondary" size="sm"><Undo /> Undo</Button>
  <ButtonGroup.Separator />
  <Button variant="soft" color="secondary" size="sm">Redo <Redo /></Button>
</ButtonGroup>`}>
  <ButtonGroupSeparatorDemo />
</ComponentPreview>

### Split

<ComponentPreview details={<>The classic split button: an action joined to a secondary affordance via <code>ButtonGroup.Separator</code>. Useful for Save / Save-As, Filter / Add-filter, Delete / Undo.</>} code={`<ButtonGroup>
  <Button variant="soft" color="secondary"><Filter /> Filter</Button>
  <ButtonGroup.Separator />
  <Button variant="soft" color="secondary" uniform aria-label="Add filter">
    <Plus />
  </Button>
</ButtonGroup>`}>
  <ButtonGroupSplitDemo />
</ComponentPreview>

### Input

<ComponentPreview details={<>Join an <code>Input</code> to a trailing <code>Button</code> for inline search, token entry, or URL shorteners. The input flexes to fill the row; the button hugs its content.</>} code={`<ButtonGroup>
  <Input placeholder="Search docs..." />
  <Button aria-label="Search"><SearchSm /></Button>
</ButtonGroup>`}>
  <ButtonGroupInputDemo />
</ComponentPreview>

### Input group

<ComponentPreview details={<>Richer composition for message composers: wrap each slot (leading action, flexible <code>Input</code>) in its own <code>ButtonGroup</code> so the outer group inserts a gap between them. The voice toggle rides inside the input via <code>endAdornment</code>. Use <code>Input.AdornmentButton</code> — it sizes off the input's own size token so the button stays concentric with the pill at every size.</>} code={`<ButtonGroup>
  <ButtonGroup>
    <Button pill uniform aria-label="Add attachment"><Plus /></Button>
  </ButtonGroup>
  <ButtonGroup>
    <Input
      pill
      placeholder={voiceEnabled ? "Recording your idea…" : "Describe a component…"}
      disabled={voiceEnabled}
      endAdornment={
        <Input.AdornmentButton
          color={voiceEnabled ? "caution" : undefined}
          aria-label="Toggle voice mode"
          aria-pressed={voiceEnabled}
          onClick={() => setVoiceEnabled((prev) => !prev)}
        >
          <Mic />
        </Input.AdornmentButton>
      }
    />
  </ButtonGroup>
</ButtonGroup>`}>
  <ButtonGroupInputGroupDemo />
</ComponentPreview>

### Dropdown menu

<ComponentPreview details={<>Compose a primary action with a <a href="/components/menu"><code>Menu</code></a> trigger for follow-up options. Common for Export / Format, Save / Save-As, Share / ….</>} code={`<ButtonGroup>
  <Button>Export</Button>
  <Menu>
    <Menu.Trigger>
      <Button uniform aria-label="More export options"><ChevronDownMd /></Button>
    </Menu.Trigger>
    <Menu.Content align="end">
      <Menu.Item><FileImage /> PNG image</Menu.Item>
      <Menu.Item><FileCode /> SVG vector</Menu.Item>
      <Menu.Item><FileDocument /> PDF document</Menu.Item>
      <Menu.Separator />
      <Menu.Item><Copy /> Copy as code</Menu.Item>
      <Menu.Item><Link /> Copy share link</Menu.Item>
    </Menu.Content>
  </Menu>
</ButtonGroup>`}>
  <ButtonGroupDropdownMenuDemo />
</ComponentPreview>

### Select

<ComponentPreview details={<>Mix a <a href="/components/select"><code>Select</code></a> and an <code>Input</code> inside one <code>ButtonGroup</code> for unit pickers, currency entry, or any "qualifier + value" pattern. Wrap each slot in a nested <code>ButtonGroup</code> to get a gap between the compound field and the send affordance.</>} code={`<ButtonGroup>
  <ButtonGroup>
    <Select options={units} value={unit} onChange={(o) => setUnit(o.value)} variant="outline" pill={false} block={false} />
    <Input placeholder="2.50" inputMode="decimal" />
  </ButtonGroup>
  <ButtonGroup>
    <Button uniform aria-label="Log weight"><ArrowRight /></Button>
  </ButtonGroup>
</ButtonGroup>`}>
  <ButtonGroupSelectDemo />
</ComponentPreview>

### Popover

<ComponentPreview details={<>Same split-button shape, but the trailing trigger opens a <a href="/components/popover"><code>Popover</code></a> instead of a menu — useful for richer panels (prompt composers, notification preferences, link previews).</>} code={`<ButtonGroup>
  <Button><Sparkles /> Generate</Button>
  <Popover>
    <Popover.Trigger>
      <Button uniform aria-label="More generate options"><ChevronDownMd /></Button>
    </Popover.Trigger>
    <Popover.Content side="bottom" align="end" className="p-4">
      <Card variant="ghost" size="sm">
        <Card.Header>
          <Card.Title>Generate a component</Card.Title>
          <Card.Description>Describe what to build in plain English.</Card.Description>
        </Card.Header>
        <Card.Content>
          <Textarea placeholder="A login form with email and password…" rows={3} />
        </Card.Content>
        <Card.Footer>
          <Card.Description>Plex will draft the code and preview it live.</Card.Description>
        </Card.Footer>
      </Card>
    </Popover.Content>
  </Popover>
</ButtonGroup>`}>
  <ButtonGroupPopoverDemo />
</ComponentPreview>

## API

### ButtonGroup

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `orientation` | `"horizontal" \| "vertical"` | `"horizontal"` | Direction of the joined items |
| `className` | `string` | — | Applied to the root `<div>` |

Renders a `<div role="group">`. All other `<div>` attributes are forwarded.

### ButtonGroup.Separator

Inherits from [`Separator`](/components/separator). `orientation` defaults to `"vertical"` (the axis that looks right between horizontal items); pass an explicit value inside a `ButtonGroup orientation="vertical"`.

### ButtonGroup.Text

Renders a muted label pill that slots into the group alongside buttons. Use it for prefixes ("@", "https://"), currency hints, or any non-interactive label that should share the joined shape.

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `asChild` | `boolean` | `false` | Render as the passed child (e.g. a `<label>` tied to an input) |
| `className` | `string` | — | Applied to the wrapper |

## Styling

Nesting `ButtonGroup` inside a parent `ButtonGroup` automatically introduces a gap between the sub-groups. The group-specific tokens:

- `--button-group-nested-gap` — space between sibling sub-groups (defaults to `spacing(2)` = 8px)
- `--button-group-separator-color` — color of the `ButtonGroup.Separator` line
- `--button-group-text-background` / `--button-group-text-color` / `--button-group-text-border-color` — surface, text, and 1px inset border of `ButtonGroup.Text`
- `--button-group-text-font-size` / `--button-group-text-font-weight` — typography of `ButtonGroup.Text`

Radius, border, and focus styles of joined buttons come from each child control's own tokens (`--button-radius`, `--input-radius`, etc.) — the group only strips the shared edge radii so joined items align.
