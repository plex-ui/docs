---
title: Markdown
description: Render rich formatted content
---

import { Markdown } from "@plexui/ui/components/Markdown"
import { markdownContent, markdownWithLatex } from "@/components/docs/MarkdownSamples"
import { ComponentPreview } from "@/components/docs/ComponentPreview"
import { ComponentSource } from "@/components/docs/ComponentSource"

<UsageBlock>{`import { Markdown } from "@plexui/ui/components/Markdown";`}</UsageBlock>

## Examples

### Overview

<ComponentPreview details={<>Use <code>Markdown</code> to render rich markdown text.</>} code={`<Markdown>{markdownContent}</Markdown>`} previewMinHeight={168}>
  <div className="w-full max-w-[700px] mx-auto py-8">
    <Markdown>{markdownContent}</Markdown>
  </div>
</ComponentPreview>

### Math

<ComponentPreview details={<>Use <code>includeMath</code> to enable LaTeX rendering.</>} code={`<Markdown includeMath>{markdownWithLatex}</Markdown>`} previewMinHeight={168}>
  <div className="w-full max-w-[700px] mx-auto py-8">
    <Markdown includeMath>{markdownWithLatex}</Markdown>
  </div>
</ComponentPreview>

## Reference

<PropsTable data={[
  { prop: "children", type: "string", description: "Markdown source" },
  { prop: "includeMath", type: "boolean", default: "false", description: "Enable LaTeX math rendering" },
]} />
