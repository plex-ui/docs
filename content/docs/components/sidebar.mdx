---
title: Sidebar
description: Composable navigation sidebar for application layouts
---

import {
  SidebarBaseDemo,
  SidebarConsoleDemo,
  SidebarNestedDemo,
  SidebarFilteredTreeDemo,
  SidebarActionTreeDemo,
  SidebarSearchDemo,
  SidebarMobileDemo,
} from "@/components/docs/SidebarDemos"
import { PREVIEW_MIN_HEIGHT } from "@/components/docs/previewMinHeights"

<UsageBlock>{`import {
  SidebarProvider,
  SidebarLayout,
  Sidebar,
  SidebarHeader,
  SidebarContent,
  SidebarFooter,
  SidebarGroup,
  SidebarGroupLabel,
  SidebarMenu,
  SidebarMenuItem,
  SidebarMenuButton,
  SidebarInset,
  SidebarTrigger,
} from "@plexui/ui/components/Sidebar";`}</UsageBlock>

## Examples

### App Shell

<ComponentPreview details={<>A multi-section app shell with a workspace selector, expandable parent sections with sub-items, and a footer with a documentation link plus a user menu. When collapsed (icon mode), hovering a parent icon opens a <code>Popover</code> flyout listing the section&apos;s items so users keep one-click access without expanding the rail.</>} code={`<SidebarProvider collapsible="icon">
  <SidebarLayout style={{ height: 600 }}>
    <Sidebar>
      <SidebarHeader>
        <span>Console</span>
        <SidebarTrigger />
      </SidebarHeader>
      <SidebarContent>
        {/* Workspace selector */}
        <SidebarMenuItem>
          <Menu>
            <Menu.Trigger>
              <SidebarMenuButton tooltip="Default workspace">
                <SidebarMenuButtonIcon><Cube /></SidebarMenuButtonIcon>
                <SidebarMenuButtonLabel>Default</SidebarMenuButtonLabel>
                <ChevronUpDown className="ml-auto" />
              </SidebarMenuButton>
            </Menu.Trigger>
            <Menu.Content>...</Menu.Content>
          </Menu>
        </SidebarMenuItem>

        {/* Parent section: hover flyout when collapsed */}
        {state === "collapsed" ? (
          <Popover showOnHover>
            <Popover.Trigger>
              <SidebarMenuButton>
                <SidebarMenuButtonIcon><SettingsWrench /></SidebarMenuButtonIcon>
                <SidebarMenuButtonLabel>Build</SidebarMenuButtonLabel>
                <SidebarMenuChevron />
              </SidebarMenuButton>
            </Popover.Trigger>
            <Popover.Content side="right" minWidth={220}>
              <div>Build / Workbench / Files / Skills / Batches</div>
            </Popover.Content>
          </Popover>
        ) : (
          <SidebarMenuItem expanded={isExpanded}>
            <SidebarMenuButton onClick={toggle}>
              <SidebarMenuButtonIcon><SettingsWrench /></SidebarMenuButtonIcon>
              <SidebarMenuButtonLabel>Build</SidebarMenuButtonLabel>
              <SidebarMenuChevron />
            </SidebarMenuButton>
            <SidebarMenuSub open={isExpanded} hasIcons>
              <SidebarMenuSubButton indent={1}>Workbench</SidebarMenuSubButton>
              ...
            </SidebarMenuSub>
          </SidebarMenuItem>
        )}
      </SidebarContent>
      <SidebarFooter>
        <SidebarMenuButton><Book /> Documentation</SidebarMenuButton>
        <Menu>
          <Menu.Trigger>
            <SidebarMenuButton>
              <Avatar name="Alex" />
              <SidebarMenuButtonLabel>Alex · Admin</SidebarMenuButtonLabel>
            </SidebarMenuButton>
          </Menu.Trigger>
          <Menu.Content>...</Menu.Content>
        </Menu>
      </SidebarFooter>
    </Sidebar>
    <SidebarInset>...</SidebarInset>
  </SidebarLayout>
</SidebarProvider>`} previewMinHeight={PREVIEW_MIN_HEIGHT.sidebarExtraTall}>
  <SidebarConsoleDemo />
</ComponentPreview>

### Basic

<ComponentPreview code={`<SidebarProvider>
  <SidebarLayout style={{ height: 500 }}>
    <Sidebar>
      <SidebarContent>
        <SidebarGroup>
          <SidebarGroupLabel size="sm">Project</SidebarGroupLabel>
          <SidebarGroupContent>
            <SidebarMenu>
              <SidebarMenuItem>
                <SidebarMenuButton isActive tooltip="Overview">
                  <SidebarMenuButtonIcon><Home /></SidebarMenuButtonIcon>
                  <SidebarMenuButtonLabel>Overview</SidebarMenuButtonLabel>
                </SidebarMenuButton>
              </SidebarMenuItem>
              ...
            </SidebarMenu>
          </SidebarGroupContent>
        </SidebarGroup>
      </SidebarContent>
      <SidebarFooter><SidebarTrigger /></SidebarFooter>
    </Sidebar>
    <SidebarInset>...</SidebarInset>
  </SidebarLayout>
</SidebarProvider>`} previewMinHeight={PREVIEW_MIN_HEIGHT.sidebarStandard}>
  <SidebarBaseDemo />
</ComponentPreview>

### Collapsible Sections

<ComponentPreview details={<>Build collapsible nested menus with chevron toggles for documentation and API reference sidebars.</>} code={`<SidebarProvider collapsible="none">
  <SidebarLayout style={{ height: 500 }}>
    <Sidebar variant="docs" style={{ width: "260px" }}>
      <SidebarContent>
        <SidebarGroup>
          <SidebarMenuItem expanded={isExpanded}>
            <SidebarMenuButton onClick={() => toggle(id)}>
              <SidebarMenuButtonLabel>Getting Started</SidebarMenuButtonLabel>
              <ChevronRightSm className="ml-auto transition-transform" />
            </SidebarMenuButton>
          </SidebarMenuItem>
          <SidebarMenuSub>
            <SidebarMenuSubItem>
              <SidebarMenuSubButton isActive>Introduction</SidebarMenuSubButton>
            </SidebarMenuSubItem>
            ...
          </SidebarMenuSub>
        </SidebarGroup>
      </SidebarContent>
    </Sidebar>
    <SidebarInset>...</SidebarInset>
  </SidebarLayout>
</SidebarProvider>`} previewMinHeight={PREVIEW_MIN_HEIGHT.sidebarTall}>
  <SidebarNestedDemo />
</ComponentPreview>

### Search and Filter

<ComponentPreview details={<>Combine search and type-based filtering with nested tree navigation. Each item has an icon based on its type. A multi-select <code>Select</code> filters items by type.</>} code={`<SidebarProvider collapsible="none">
  <SidebarLayout style={{ height: 650 }}>
    <Sidebar style={{ width: "280px" }}>
      <SidebarHeader>
        <div className="flex items-center gap-1.5">
          <SidebarInput size="sm" placeholder="Search" ... />
          <Select
            multiple
            clearable
            placeholder="Filter"
            variant="outline"
            size="sm"
            options={stepFilterOptions}
            value={typeFilters}
            onChange={(selected) =>
              setTypeFilters(selected.map((o) => o.value))
            }
            block={false}
          />
        </div>
      </SidebarHeader>
      <SidebarContent>
        <SidebarGroup>
          <SidebarGroupContent>
            <SidebarMenu>
              <SidebarMenuItem>
                <SidebarMenuButton isActive>
                  <SidebarMenuButtonIcon><Desktop /></SidebarMenuButtonIcon>
                  <SidebarMenuButtonLabel>Start</SidebarMenuButtonLabel>
                </SidebarMenuButton>
              </SidebarMenuItem>
              ...
            </SidebarMenu>
          </SidebarGroupContent>
        </SidebarGroup>
      </SidebarContent>
    </Sidebar>
    <SidebarInset>...</SidebarInset>
  </SidebarLayout>
</SidebarProvider>`} previewMinHeight={PREVIEW_MIN_HEIGHT.sidebarExtraTall}>
  <SidebarFilteredTreeDemo />
</ComponentPreview>

### Tree with Hover Actions

<ComponentPreview details={<>Tree navigation with chevrons on the left and action buttons on the right. Clicking a row selects the item. Chevrons toggle expand/collapse. Action buttons (<code>...</code> and <code>+</code>) appear on hover for parent items.</>} code={`<SidebarMenuItem className="group/action">
  <SidebarMenuButton isActive onClick={() => setActiveItem(id)}>
    <SidebarMenuChevron className="!ml-0 ..."
      onClick={(e) => { e.stopPropagation(); toggleExpanded(id); }}
    />
    <SidebarMenuButtonIcon><Icon /></SidebarMenuButtonIcon>
    <SidebarMenuButtonLabel>Label</SidebarMenuButtonLabel>
    <span className="ml-auto opacity-0 group-hover/action:opacity-100">
      <Button variant="ghost" size="xs" uniform>
        <DotsHorizontal />
      </Button>
      <Button variant="ghost" size="xs" uniform>
        <Plus />
      </Button>
    </span>
  </SidebarMenuButton>
  <SidebarMenuSub open hasIcons>...</SidebarMenuSub>
</SidebarMenuItem>`} previewMinHeight={PREVIEW_MIN_HEIGHT.sidebarExtraTall}>
  <SidebarActionTreeDemo />
</ComponentPreview>

### Mobile Menu

<ComponentPreview details={<>Mobile behavior is handled automatically when using <code>SidebarProvider</code>. The <code>SidebarMobileMenuButton</code> provides a standard animated hamburger trigger.</>} code={`// Desktop: standard sidebar with icon collapse
<SidebarProvider collapsible="icon">
  <SidebarLayout style={{ height: 500 }}>
    <Sidebar>...</Sidebar>
    <SidebarInset>...</SidebarInset>
  </SidebarLayout>
</SidebarProvider>

// Mobile: phone-sized container with hamburger menu
<SidebarProvider collapsible="offcanvas">
  <header>
    <SidebarMobileMenuButton />
  </header>
  {openMobile ? <SidebarContent>...</SidebarContent> : <div>Page content</div>}
</SidebarProvider>`} previewMinHeight={PREVIEW_MIN_HEIGHT.sidebarExtraTall}>
  <SidebarMobileDemo />
</ComponentPreview>


### Search Input

<ComponentPreview details={<>The sidebar provides a dedicated <code>SidebarInput</code> component for search functionality.</>} code={`<SidebarProvider collapsible="none">
  <SidebarLayout style={{ height: 500 }}>
    <Sidebar variant="docs" style={{ width: "220px" }}>
      <SidebarHeader>
        <SidebarInput
          placeholder="Search"
          value={searchValue}
          onChange={(e) => setSearchValue(e.target.value)}
          onClear={() => setSearchValue("")}
        />
      </SidebarHeader>
      <SidebarContent>
        <SidebarGroup>
          <SidebarGroupLabel>Get started</SidebarGroupLabel>
          <SidebarGroupContent>
            <SidebarMenu>
              <SidebarMenuItem>
                <SidebarMenuSubButton indent={0} isActive>Overview</SidebarMenuSubButton>
              </SidebarMenuItem>
              ...
            </SidebarMenu>
          </SidebarGroupContent>
        </SidebarGroup>
      </SidebarContent>
    </Sidebar>
    <SidebarInset>...</SidebarInset>
  </SidebarLayout>
</SidebarProvider>`} previewMinHeight={PREVIEW_MIN_HEIGHT.sidebarStandard}>
  <SidebarSearchDemo />
</ComponentPreview>

## Reference

### Structure

A Sidebar is composed of the following parts:

- `SidebarProvider` &ndash; Handles collapsible state and provides context to all child components.
- `SidebarLayout` &ndash; Wrapper for sidebar and main content.
- `Sidebar` &ndash; The sidebar container.
- `SidebarHeader` / `SidebarFooter` &ndash; Sticky at the top and bottom of the sidebar.
- `SidebarContent` &ndash; Scrollable content area.
- `SidebarGroup` &ndash; Section within the content, with optional label.
- `SidebarMenu` / `SidebarMenuItem` / `SidebarMenuButton` &ndash; Navigation menu items.
- `SidebarMenuSub` / `SidebarMenuSubButton` &ndash; Nested submenu items.
- `SidebarInset` &ndash; Main content area next to the sidebar.
- `SidebarTrigger` &ndash; Toggle button for collapsing the sidebar.

Use `Cmd+B` (Mac) or `Ctrl+B` (Windows/Linux) to toggle the sidebar with keyboard.

### SidebarProvider

Handles sidebar state and provides context to all child components. Wrap your application layout in this provider.

<PropsTable data={[
  { prop: "collapsible", type: '"offcanvas" | "icon" | "none"', default: '"offcanvas"', description: "Collapse behavior: offcanvas (drawer on mobile, full collapse on desktop), icon (icon rail), or none" },
  { prop: "defaultOpen", type: "boolean", default: "true", description: "Initial open state (uncontrolled)" },
  { prop: "open", type: "boolean", description: "Controlled open state" },
  { prop: "onOpenChange", type: "(open: boolean) => void", description: "Called when open state changes" },
]} />

### Sidebar

The main sidebar container. Renders as an `<aside>` element.

<PropsTable data={[
  { prop: "variant", type: '"sidebar" | "docs"', default: '"sidebar"', description: "Visual variant" },
  { prop: "side", type: '"left" | "right"', default: '"left"', description: "Which side the sidebar is on" },
]} />

### SidebarMenuButton

Renders a menu item button. Use `isActive` to highlight the current page.

<PropsTable data={[
  { prop: "isActive", type: "boolean", default: "false", description: "Highlight the current page" },
  { prop: "tooltip", type: "string | ReactNode", description: "Label shown on hover when collapsed (icon mode). Hidden on mobile." },
]} />

### SidebarGroupLabel

Renders a section label within a `SidebarGroup`. Supports two size variants.

<PropsTable data={[
  { prop: "size", type: '"sm" | "lg"', default: '"lg"', description: "sm — compact, tertiary; lg — larger, primary, more spacing" },
]} />

### SidebarMenuSub

Renders a collapsible submenu container for nested navigation.

<PropsTable data={[
  { prop: "open", type: "boolean", default: "true", description: "Whether submenu is expanded" },
  { prop: "hasIcons", type: "boolean", default: "false", description: "Align child items with parent text that has icons" },
]} />

### SidebarCard

Renders a promotional card in the sidebar footer for CTAs, upgrades, or announcements.

<PropsTable data={[
  { prop: "dismissible", type: "boolean", description: "Shows dismiss button" },
  { prop: "onDismiss", type: "() => void", description: "Callback when dismissed" },
]} />

### useSidebar

Hook to access sidebar state and controls from any component within `SidebarProvider`.

- `state` &ndash; Current state: `"expanded"` or `"collapsed"`
- `open` / `setOpen` &ndash; Desktop sidebar open state
- `openMobile` / `setOpenMobile` &ndash; Mobile menu open state
- `isMobile` &ndash; Whether the viewport is mobile
- `collapsible` &ndash; Current collapsible mode
- `toggleSidebar` &ndash; Toggle function for both desktop and mobile
