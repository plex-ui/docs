---
title: Card
description: Surface primitive for composing forms, stats, and showcase tiles
---

import { Card } from "@plexui/ui/components/Card"
import { Button } from "@plexui/ui/components/Button"
import {
  CardOverviewDemo,
  CardLoginDemo,
  CardCreateProjectDemo,
  CardPaymentMethodDemo,
  CardNoTeamMembersDemo,
  CardSurveyDemo,
  CardProcessingDemo,
  CardPricingDemo,
} from "@/components/docs/CardDemos"

<UsageBlock>{`import { Card } from "@plexui/ui/components/Card";`}</UsageBlock>

## Examples

<div id="first-component-page">
### Overview

<ComponentPreview code={`<Card>
  <Card.Header>
    <Card.Title>Project activity</Card.Title>
    <Card.Description>
      Your team opened 12 issues and closed 8 pull requests over the last 7 days.
    </Card.Description>
  </Card.Header>
  <Card.Footer>
    <Button color="primary" size="sm" pill={false}>View report</Button>
    <Button color="secondary" variant="outline" size="sm" pill={false}>Dismiss</Button>
  </Card.Footer>
</Card>`}>
  <CardOverviewDemo />
</ComponentPreview>

### Login

<ComponentPreview details={<>A classic login form with a primary <strong>Log in</strong> CTA and an inline <em>Sign up</em> link below.</>} code={`<Card>
  <Card.Header>
    <Card.Title>Log in to your account</Card.Title>
    <Card.Description>Enter your email below to log in to your account.</Card.Description>
  </Card.Header>
  <Card.Content>
    <Field label="Email"><Input type="email" placeholder="you@example.com" /></Field>
    <Field label="Password"><Input type="password" /></Field>
  </Card.Content>
  <Card.Footer>
    <Button color="primary" block pill={false}>Log in</Button>
  </Card.Footer>
</Card>`}>
  <CardLoginDemo />
</ComponentPreview>

### Create project

<ComponentPreview details={<>Right-aligned footer with Cancel / Submit — use <code>style=\{{justifyContent: 'flex-end'}}</code> on <code>Card.Footer</code>.</>} code={`<Card>
  <Card.Header>
    <Card.Title>Create project</Card.Title>
    <Card.Description>Deploy a new project in one-click with sensible defaults.</Card.Description>
  </Card.Header>
  <Card.Content>
    <Field label="Name"><Input placeholder="billing-service" /></Field>
    <Field label="Region" description="Where your functions and database will live.">
      <Input placeholder="us-east-1" />
    </Field>
  </Card.Content>
  <Card.Footer style={{ justifyContent: 'flex-end' }}>
    <Button variant="outline" size="sm" pill={false}>Cancel</Button>
    <Button color="primary" size="sm" pill={false}>Deploy</Button>
  </Card.Footer>
</Card>`}>
  <CardCreateProjectDemo />
</ComponentPreview>

### Payment Method

<ComponentPreview details={<>Full-fidelity payment form. <strong>Card Number</strong> + <strong>CVV</strong> share a 2fr/1fr row with a helper line; <strong>Month</strong>/<strong>Year</strong> use <code>Select</code>. Sections are divided by subtle <code>hr</code>s for Billing Address and Comments.</>} code={`<Card>
  <Card.Header>
    <Card.Title>Payment Method</Card.Title>
    <Card.Description>All transactions are secure and encrypted</Card.Description>
  </Card.Header>
  <Card.Content>
    <Field label="Name on Card"><Input placeholder="John Doe" /></Field>
    {/* Card Number (2fr) + CVV (1fr) */}
    <Field label="Card Number" description="Enter your 16-digit number.">
      <Input placeholder="1234 5678 9012 3456" />
    </Field>
    <Field label="CVV"><Input placeholder="123" /></Field>
    {/* Month + Year as Select dropdowns */}
    <Field label="Month"><Select options={MONTH_OPTIONS} /></Field>
    <Field label="Year"><Select options={YEAR_OPTIONS} /></Field>
    <hr />
    <section>
      <h4>Billing Address</h4>
      <p>The billing address associated with your payment method</p>
      <Checkbox>Same as shipping address</Checkbox>
    </section>
    <hr />
    <Field label="Comments"><Textarea placeholder="Add any additional comments" /></Field>
  </Card.Content>
  <Card.Footer>
    <Button color="primary" pill={false}>Submit</Button>
    <Button variant="outline" pill={false}>Cancel</Button>
  </Card.Footer>
</Card>`}>
  <CardPaymentMethodDemo />
</ComponentPreview>

### No team members (empty state)

<ComponentPreview details={<>Compose <code>Card</code> with <code>EmptyMessage</code> for a center-aligned empty state with icon badge, title, description, and primary action.</>} code={`<Card>
  <EmptyMessage>
    <EmptyMessage.Icon><UserAdd /></EmptyMessage.Icon>
    <EmptyMessage.Title>No team members</EmptyMessage.Title>
    <EmptyMessage.Description>Invite your team to collaborate on this project.</EmptyMessage.Description>
    <EmptyMessage.ActionRow>
      <Button color="primary" size="sm" pill={false}>Invite members</Button>
    </EmptyMessage.ActionRow>
  </EmptyMessage>
</Card>`}>
  <CardNoTeamMembersDemo />
</ComponentPreview>

### How did you hear about us?

<ComponentPreview details={<>A single-question survey using <code>RadioGroup</code> with <code>direction="col"</code> and a full-width submit button.</>} code={`<Card>
  <Card.Header>
    <Card.Title>How did you hear about us?</Card.Title>
    <Card.Description>Select the option that best describes how you heard about us.</Card.Description>
  </Card.Header>
  <Card.Content>
    <RadioGroup value={source} onChange={setSource} direction="col">
      <RadioGroup.Item value="social" block>Social Media</RadioGroup.Item>
      <RadioGroup.Item value="search" block>Search Engine</RadioGroup.Item>
      <RadioGroup.Item value="referral" block>Referral</RadioGroup.Item>
      <RadioGroup.Item value="other" block>Other</RadioGroup.Item>
    </RadioGroup>
  </Card.Content>
  <Card.Footer><Button color="primary" block pill={false}>Submit</Button></Card.Footer>
</Card>`}>
  <CardSurveyDemo />
</ComponentPreview>

### Processing your request

<ComponentPreview details={<>Loading state built with <code>EmptyMessage</code> and a <code>LoadingIndicator</code> as the visual anchor.</>} code={`<Card>
  <EmptyMessage>
    <EmptyMessage.Icon><LoadingIndicator size={24} /></EmptyMessage.Icon>
    <EmptyMessage.Title>Processing your request</EmptyMessage.Title>
    <EmptyMessage.Description>Please wait while we process your request. Do not refresh the page.</EmptyMessage.Description>
    <EmptyMessage.ActionRow>
      <Button variant="outline" size="sm" pill={false}>Cancel</Button>
    </EmptyMessage.ActionRow>
  </EmptyMessage>
</Card>`}>
  <CardProcessingDemo />
</ComponentPreview>

### Pricing

<ComponentPreview details={<>Mix <code>variant="outline"</code> and <code>variant="solid"</code> to emphasize a highlighted plan.</>} code={`<Card variant="solid">
  <Card.Header>
    <Card.Title>Pro</Card.Title>
    <Card.Description>$19 / month</Card.Description>
  </Card.Header>
  <Card.Content>{/* features list */}</Card.Content>
  <Card.Footer>
    <Button color="primary" block pill={false}>Choose Pro</Button>
  </Card.Footer>
</Card>`}>
  <CardPricingDemo />
</ComponentPreview>
</div>

## Anatomy

```tsx
<Card>
  <Card.Header>
    <Card.Title />
    <Card.Description />
    <Card.Action />  {/* optional top-right slot */}
  </Card.Header>
  <Card.Content />
  <Card.Footer />
</Card>
```

## API

### Card

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `variant` | `"outline" \| "solid" \| "ghost"` | `"outline"` | Visual variant |
| `size` | `"sm" \| "md" \| "lg"` | `"md"` | Padding scale |
| `interactive` | `boolean` | `false` | Adds hover and focus affordances. Automatic when `href` is set. |
| `href` | `string` | — | Renders the Card as a link via the configured `LinkComponent`. |
| `external` | `boolean` | auto-detected | Forces `target="_blank"` and `rel="noopener noreferrer"` when `href` is set. |
| `as` | `ComponentType \| "a"` | — | Overrides the link component (e.g. `next/link`). |
| `className` | `string` | — | Applied to the root element |

All other `<div>` or `<a>` attributes are forwarded to the root.

### Compound parts

| Part | Element | Purpose |
|------|---------|---------|
| `Card.Header` | `<div>` | Title + description + optional action |
| `Card.Title` | `<h3>` | Card heading |
| `Card.Description` | `<p>` | Subtitle / explanation |
| `Card.Content` | `<div>` | Primary body region |
| `Card.Footer` | `<div>` | Trailing actions row |
| `Card.Action` | `<div>` | Absolute top-right slot inside `Card.Header` |
