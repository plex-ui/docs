---
title: Radio Group
description: Semantic radio option selection
---

import {
  RadioGroupBaseDemo,
  RadioGroupDirectionDemo,
  RadioGroupOrientationDemo,
  RadioGroupDisabledDemo,
  RadioGroupItemDisabledDemo,
  RadioGroupWithDescriptionDemo,
  RadioGroupWithFieldDemo,
  RadioGroupWithFieldErrorDemo,
  RadioGroupChoiceCardDemo,
  RadioGroupCardSelectionDemo,
} from "@/components/docs/RadioGroupDemos"

<UsageBlock>{`import { RadioGroup } from "@plexui/ui/components/RadioGroup";`}</UsageBlock>

## Examples

### Overview

<ComponentPreview code={`const [frequency, setFrequency] = useState("daily")
<RadioGroup direction="col" value={frequency} onChange={setFrequency} aria-label="Notification frequency">
  <RadioGroup.Item value="daily">Daily</RadioGroup.Item>
  <RadioGroup.Item value="weekly">Weekly</RadioGroup.Item>
  <RadioGroup.Item value="monthly">Monthly</RadioGroup.Item>
  <RadioGroup.Item value="never">Never</RadioGroup.Item>
</RadioGroup>`} previewMinHeight={168}>
  <RadioGroupBaseDemo />
</ComponentPreview>

### Direction

<ComponentPreview details={<>Use <code>direction</code> to control whether the items flow in a column or row. An optimal <code>gap</code> will be dynamically applied based on the orientation.</>} code={`<RadioGroup direction="col" defaultValue="light" aria-label="Theme">
  <RadioGroup.Item value="light">Light</RadioGroup.Item>
  <RadioGroup.Item value="dark">Dark</RadioGroup.Item>
  <RadioGroup.Item value="system">System</RadioGroup.Item>
</RadioGroup>`} previewMinHeight={168}>
  <RadioGroupDirectionDemo />
</ComponentPreview>

### Orientation

<ComponentPreview details={<>Use <code>orientation="right"</code> on <code>RadioGroup.Item</code> to place the radio indicator after the label text. Useful for settings lists and card-style selections where the content should lead.</>} code={`<RadioGroup className="w-[320px]" direction="col" defaultValue="email" aria-label="Default notification channel">
  <RadioGroup.Item value="email" orientation="right" block>Email</RadioGroup.Item>
  <RadioGroup.Item value="sms" orientation="right" block>SMS</RadioGroup.Item>
  <RadioGroup.Item value="push" orientation="right" block>Push notification</RadioGroup.Item>
</RadioGroup>`} previewMinHeight={168}>
  <RadioGroupOrientationDemo />
</ComponentPreview>

### Group disabled

<ComponentPreview details={<>Use <code>disabled</code> on <code>RadioGroup</code> to disable all radio options.</>} code={`<RadioGroup disabled defaultValue="standard" aria-label="Shipping method">
  <RadioGroup.Item value="standard">Standard</RadioGroup.Item>
  <RadioGroup.Item value="express">Express</RadioGroup.Item>
  <RadioGroup.Item value="overnight">Overnight</RadioGroup.Item>
</RadioGroup>`} previewMinHeight={168}>
  <RadioGroupDisabledDemo />
</ComponentPreview>

### Item disabled

<ComponentPreview details={<>Use <code>disabled</code> on single <code>RadioGroup.Item</code> to disable only that option.</>} code={`<RadioGroup aria-label="Delivery method" direction="col">
  <RadioGroup.Item value="standard">Standard shipping (3–5 days)</RadioGroup.Item>
  <RadioGroup.Item value="express" disabled>Express shipping (1–2 days)</RadioGroup.Item>
  <RadioGroup.Item value="same-day">
    Same-day delivery — available in select metro areas with a minimum order of $50
  </RadioGroup.Item>
</RadioGroup>`} previewMinHeight={168}>
  <RadioGroupItemDisabledDemo />
</ComponentPreview>

### With description

<ComponentPreview details={<>Radio items accept <code>ReactNode</code> children, so you can compose a label with secondary description text — useful for options that need additional context.</>} code={`<RadioGroup direction="col" value={value} onChange={setValue} aria-label="Spacing">
  <RadioGroup.Item value="default">
    <div className="flex flex-col">
      <span>Default</span>
      <span className="text-xs text-secondary mt-0.5">
        Standard spacing for most use cases.
      </span>
    </div>
  </RadioGroup.Item>
  <RadioGroup.Item value="comfortable">
    <div className="flex flex-col">
      <span>Comfortable</span>
      <span className="text-xs text-secondary mt-0.5">
        More space between elements.
      </span>
    </div>
  </RadioGroup.Item>
  <RadioGroup.Item value="compact">
    <div className="flex flex-col">
      <span>Compact</span>
      <span className="text-xs text-secondary mt-0.5">
        Minimal spacing for dense layouts.
      </span>
    </div>
  </RadioGroup.Item>
</RadioGroup>`} previewMinHeight={200}>
  <RadioGroupWithDescriptionDemo />
</ComponentPreview>

### With Field

<ComponentPreview details={<>Wrap a <code>RadioGroup</code> in a <code>Field</code> for a shared label and description. The Field provides <code>aria-describedby</code> linking automatically.</>} code={`<Field label="Notification method" description="Choose how you want to receive notifications.">
  {() => (
    <RadioGroup direction="col" value={value} onChange={setValue} aria-label="Notification method">
      <RadioGroup.Item value="email">Email</RadioGroup.Item>
      <RadioGroup.Item value="sms">SMS</RadioGroup.Item>
      <RadioGroup.Item value="push">Push notification</RadioGroup.Item>
    </RadioGroup>
  )}
</Field>`} previewMinHeight={200}>
  <RadioGroupWithFieldDemo />
</ComponentPreview>

### With Field error

<ComponentPreview details={<>When a <code>Field</code> has an <code>errorMessage</code>, it sets <code>aria-invalid</code> on the child control for screen readers. The error message is displayed below the group.</>} code={`<Field
  label="Subscription plan"
  description="Select a plan to continue."
  errorMessage={showError ? "Please select a subscription plan." : undefined}
>
  {(fieldProps) => (
    <RadioGroup direction="col" aria-label="Subscription plan" {...fieldProps}>
      <RadioGroup.Item value="monthly">Monthly ($9.99/month)</RadioGroup.Item>
      <RadioGroup.Item value="yearly">Yearly ($99.99/year)</RadioGroup.Item>
      <RadioGroup.Item value="lifetime">Lifetime ($299.99)</RadioGroup.Item>
    </RadioGroup>
  )}
</Field>`} previewMinHeight={240}>
  <RadioGroupWithFieldErrorDemo />
</ComponentPreview>

### Choice card

<ComponentPreview details={<>Style each radio option as a bordered card with visual selection state. The selected card gets a highlighted border using design tokens. Use <code>orientation="right"</code> to place the indicator after the content. Wrap each <code>RadioGroup.Item</code> in a card container with <code>border</code> and <code>borderRadius</code> on the same element.</>} code={`<RadioGroup className="flex-col w-[320px] gap-3" value={value} onChange={setValue} aria-label="Workspace type">
  <div style={{
    border: "1px solid",
    borderColor: isSelected ? "var(--color-background-primary-solid)" : "var(--color-border-primary-outline)",
    borderRadius: "var(--radius-lg)",
    backgroundColor: isSelected ? "var(--color-background-primary-soft-alpha)" : undefined,
  }}>
    <RadioGroup.Item value="personal" block orientation={orientation} className="gap-3 p-3">
      <div className="flex flex-col flex-1">
        <span className="font-semibold">Personal</span>
        <span className="text-xs text-secondary mt-0.5">For hobby projects and experimentation.</span>
      </div>
    </RadioGroup.Item>
  </div>
  ...
</RadioGroup>`} previewMinHeight={240}>
  <RadioGroupChoiceCardDemo />
</ComponentPreview>

### Card selection

<ComponentPreview details={<>Leverage component composition and <code>className</code> overrides to build card-style radio selections with rich content per option.</>} className="justify-start" code={`<RadioGroup className="flex-col w-[390px] gap-4">
  <RadioGroup.Item className="gap-2.5" value="basic">
    <div><h4>Basic Plan</h4><p className="text-secondary text-sm">5 GB storage</p></div>
  </RadioGroup.Item>
  ...
</RadioGroup>`} previewMinHeight={168}>
  <RadioGroupCardSelectionDemo />
</ComponentPreview>

## Reference

**RadioGroup** (root)

<PropsTable data={[
  { prop: "value", type: "string", description: "Controlled selected value" },
  { prop: "defaultValue", type: "string", description: "Initial selected value (uncontrolled)" },
  { prop: "onChange", type: "(value: string) => void", description: "Called when selected option changes" },
  { prop: "direction", type: '"col" | "row"', default: '"col"', description: "Item flow direction" },
  { prop: "disabled", type: "boolean", default: "false", description: "Disable the whole group" },
  { prop: "aria-label", type: "string", description: "Accessible group label" },
]} />

**RadioGroup.Item**

<PropsTable data={[
  { prop: "value", type: "string", description: "Item value" },
  { prop: "disabled", type: "boolean", default: "false", description: "Disable this item only" },
  { prop: "block", type: "boolean", default: "false", description: "Stretch item to fill available width" },
  { prop: "orientation", type: '"left" | "right"', default: '"left"', description: "Position of the radio indicator relative to the label" },
]} />
